import { Typography } from '@mui/material';

import { PortfolioSectionContainer } from '@/components';

export const SolutionOverviewContent = () => {
    return (
        <PortfolioSectionContainer>
            <Typography>
                Rather than designing the solution all at once, the product structure
                evolved through continuous discovery, internal testing, and airport
                validation. We began with a Domestic MVP, iteratively validated and
                refined the experience, expanded it into the complete domestic journey,
                and finally scaled it to support the full international experience before
                progressing into detailed interaction and UI design.
            </Typography>
        </PortfolioSectionContainer>
    );
};
