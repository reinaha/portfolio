import { Typography } from '@mui/material';

import { PortfolioSectionContainer } from '@/components';

export const InternalValidationOverviewContent = () => {
    return (
        <PortfolioSectionContainer>
            <Typography>
                Before public trials, the kiosk underwent multiple rounds of internal
                validation with Air New Zealand staff. Testing covered both passenger
                usability and airport operations, helping identify usability issues,
                operational gaps, and support requirements. Insights from each session
                were rapidly incorporated into the product through continuous iteration
                before launch.
            </Typography>
        </PortfolioSectionContainer>
    );
};
