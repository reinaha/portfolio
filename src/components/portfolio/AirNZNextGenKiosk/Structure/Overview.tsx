import { Typography } from '@mui/material';

import { PortfolioSectionContainer } from '@/components';

export const StructureOverviewContent = () => {
    return (
        <PortfolioSectionContainer>
            <Typography>
                Insights from Discovery were translated into the first product structure.
                Starting with a core MVP journey, we identified the screens required to
                support the airport check-in experience, defined the overall flow, and
                planned how future features would integrate into the journey before moving
                into detailed interaction and UI design.
            </Typography>
        </PortfolioSectionContainer>
    );
};
