// Password required to unlock password-gated case studies (see <PasswordGate />).
// Change this value to change the password — no other files need to be touched.
export const CASE_STUDY_PASSWORD = 'airnzsumin';
